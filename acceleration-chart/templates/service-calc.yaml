apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.accelerationCALC.service.name }}
  namespace: {{ .Values.accelerationCALC.namespace }}
  labels:
    name: {{ .Values.accelerationCALC.service.name }}
spec:
  ports:
    - name: http
      port: {{ .Values.accelerationCALC.service.servicePort }}
      protocol: TCP
  selector:
    app: {{ .Values.accelerationCALC.name }}
