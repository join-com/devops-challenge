apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "calc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "calc.name" . }}
    helm.sh/chart: {{ include "calc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "calc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
