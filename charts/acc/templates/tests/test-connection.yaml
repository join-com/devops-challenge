apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "acc.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "acc.name" . }}
    helm.sh/chart: {{ include "acc.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "acc.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
