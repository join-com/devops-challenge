apiVersion: v1
kind: Service
metadata:
  name: {{ include "acceleration-dv.fullname" . }}
  labels:
    app: {{ include "acceleration-dv.name" . }}
    chart: {{ include "acceleration-dv.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "acceleration-dv.name" . }}
    release: {{ .Release.Name }}
