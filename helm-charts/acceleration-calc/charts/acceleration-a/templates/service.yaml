apiVersion: v1
kind: Service
metadata:
  name: {{ include "acceleration-a.fullname" . }}
  labels:
    app: {{ include "acceleration-a.name" . }}
    chart: {{ include "acceleration-a.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ include "acceleration-a.name" . }}
    release: {{ .Release.Name }}
