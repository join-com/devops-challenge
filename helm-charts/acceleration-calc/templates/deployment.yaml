apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: {{ include "acceleration-calc.fullname" . }}
  labels:
    app: {{ include "acceleration-calc.name" . }}
    chart: {{ include "acceleration-calc.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  completions: 2
  parallelism: 2
  minReadySeconds: 30
  strategy:
    rollingUpdate:
      maxSurge: 2
    type: RollingUpdate
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "acceleration-calc.name" . }}
      release: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ include "acceleration-calc.name" . }}
        release: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.application_port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 30
            periodSeconds: 1
          readinessProbe:
            httpGet:
              path: /health
              port: http
            initialDelaySeconds: 20
            periodSeconds: 1
          env:
            - name: DV_URL
              value: "http://{{ .Release.Name }}-acceleration-dv/dv"
            - name: A_URL
              value: "http://{{ .Release.Name }}-acceleration-a/a"
          resources:
{{ toYaml .Values.resources | indent 12 }}
    {{- with .Values.nodeSelector }}
      nodeSelector:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.affinity }}
      affinity:
{{ toYaml . | indent 8 }}
    {{- end }}
    {{- with .Values.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
